import  {COLORS, FONT_SIZE} from "../../utils/constants";

export const styles = {
    banners : {
        marginTop:20,
    },
    labelBanners : {
       fontSize:FONT_SIZE.medium,
       color:COLORS.dark_gray, 
    }
}